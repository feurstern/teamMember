const object1={
    name:{
        first: 'Hatsune',
        last: 'Miku'
    },
    grades:[2,4,5,6],
    department : 'Computer Science'
}

const a = object1.name.first;
const b = object1.name.last;
object1.addresss = "Tokyo";
object1.

console.log(`The student name : ${a} ${b}`);
console.log(object1);